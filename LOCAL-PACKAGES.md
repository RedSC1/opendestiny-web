# 工具依赖

计算内核使用 npm 上已发布的固定版本，普通克隆、安装和构建不依赖本机仓库外的内核目录。

| 包 | 版本 | 许可证 |
| --- | --- | --- |
| bazi-lite | 1.1.0 | MPL-2.0 |
| ziwei-lite | 1.1.0 | MPL-2.0 |
| js-ephemeris-lite | 1.1.0 | MPL-2.0 |
| huangli-lite | 1.1.0 | MIT |

`npm run prepare:tools` 从这些 npm 包重新生成浏览器端计算文件，并把对应许可证、第三方声明和版本信息一并写入静态产物。

八字、紫微与塔罗界面通过静态工具工作区接入本站。页面迁移脚本仍从相邻的 `../webpage` 读取旧工具页面，从 `../OpenDestiny/tarot` 读取塔罗页面和图片。仓库已经提交 `public/tools` 下已验证的生成文件，因此常规站点构建不要求这两个相邻目录；只有刷新迁移后的页面与塔罗 vendor 文件时才需要它们。
